# observe data range
observe({
  
  
  updateDateRangeInput(session = session,
                       inputId = "dateRange",
                       label   = "Date Range",
                       start   = NULL,
                       end     = NULL)
})
